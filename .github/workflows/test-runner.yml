name: Test Runner Connection

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Basic Check
        run: |
          echo "Hello from runner"
          pwd
          whoami
          date 